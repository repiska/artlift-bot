# Редактирование сообщений администратором

## Описание

Администраторы могут редактировать все текстовые сообщения бота через админ-панель без изменения кода.

## Доступные шаблоны сообщений

1. **`welcome`** - Приветственное сообщение при команде `/start`
2. **`application_filled_response`** - Ответ после подтверждения заполнения анкеты
3. **`user_question_response`** - Ответ на вопрос пользователя
4. **`application_approved`** - Уведомление об одобрении заявки
5. **`application_rejected`** - Уведомление об отклонении заявки
6. **`faq`** - FAQ раздел

## Как использовать

### 1. Доступ к редактированию

1. Откройте админ-панель (кнопка "⚙️ Админ-панель")
2. Нажмите "✏️ Редактировать сообщения"
3. Выберите нужное сообщение из списка

### 2. Редактирование текста

1. Выберите сообщение для редактирования
2. Нажмите "✏️ Редактировать текст"
3. Отправьте новый текст сообщения
4. Проверьте превью
5. Нажмите "✅ Сохранить" для применения изменений

### 3. Просмотр текущего текста

1. Выберите сообщение
2. Нажмите "👁️ Просмотреть текущий текст"
3. Просмотрите содержимое

## Переменные в шаблонах

В шаблонах можно использовать следующие переменные:

- `{name}` - Имя пользователя
- `{APPLICATION_FORM_URL}` - Ссылка на анкету (из настроек)
- `{PAYMENT_URL}` - Ссылка на оплату (из настроек)
- `{CONTACT_USERNAME}` - Контакт для связи (из настроек)

### Пример использования переменных:

```
Привет, {name}!

Заполните анкету: {APPLICATION_FORM_URL}
Оплата: {PAYMENT_URL}
Вопросы: {CONTACT_USERNAME}
```

## Отмена редактирования

Во время редактирования можно отменить действие командой `/cancel`.

## Технические детали

- Все шаблоны хранятся в таблице `bot_messages` базы данных
- Изменения сохраняются с указанием администратора и времени обновления
- Все действия администраторов логируются в таблице `admin_actions`

## Безопасность

- Только администраторы могут редактировать сообщения
- Проверка прав доступа происходит на каждом этапе
- Все изменения логируются

